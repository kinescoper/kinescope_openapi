# 🤖 Автоматизация синхронизации Postman → OpenAPI

Система автоматической синхронизации документации из Postman коллекции в OpenAPI формат.

## 🎯 Возможности

- ✅ Автоматическая загрузка коллекции из Postman API
- ✅ Конвертация в OpenAPI 3.0.3
- ✅ Автоматическое обновление через GitHub Actions
- ✅ Резервное копирование коллекций
- ✅ Поддержка ручного запуска

## 📋 Настройка

### Шаг 1: Получите Postman API Key

1. Войдите в [Postman](https://www.postman.com/)
2. Перейдите в [Account Settings → API Keys](https://go.postman.com/settings/me/api-keys)
3. Создайте новый API Key или используйте существующий
4. Скопируйте ключ

### Шаг 2: Найдите Collection ID

1. Откройте вашу коллекцию в Postman
2. Нажмите на коллекцию → View → View Collection Info
3. Скопируйте **Collection ID** или **Collection UID**

### Шаг 3: Настройте конфигурацию

Создайте файл `postman-config.json` в корне проекта:

```json
{
  "postmanApiKey": "YOUR_POSTMAN_API_KEY",
  "collectionId": "YOUR_COLLECTION_ID",
  "workspaceId": "YOUR_WORKSPACE_ID"
}
```

**⚠️ Важно:** Добавьте `postman-config.json` в `.gitignore` чтобы не коммитить API ключ!

### Шаг 4: Настройте GitHub Secrets (для автоматизации)

Если используете GitHub Actions, добавьте secrets в настройках репозитория:

1. Settings → Secrets and variables → Actions
2. Добавьте:
   - `POSTMAN_API_KEY` - ваш Postman API ключ
   - `POSTMAN_COLLECTION_ID` - ID коллекции

## 🚀 Использование

### Ручной запуск

#### Вариант 1: Из локального файла
```bash
node scripts/postman-to-openapi.js /path/to/collection.json
```

#### Вариант 2: Из Postman API
```bash
# Сначала загрузите коллекцию из API
node scripts/sync-from-postman-api.js

# Затем конвертируйте
node scripts/postman-to-openapi.js
```

#### Вариант 3: Все в одном (рекомендуется)
```bash
# Загружает из API и конвертирует автоматически
POSTMAN_API_KEY=your_key POSTMAN_COLLECTION_ID=your_id node scripts/sync-and-convert.js --from-api
```

### Автоматический запуск через GitHub Actions

GitHub Actions workflow настроен на автоматический запуск:

1. **Ежедневно в 9:00 UTC** - автоматическая синхронизация
2. **При изменении скриптов** - проверка работоспособности
3. **Вручную** - через Actions → Sync Postman to OpenAPI → Run workflow

### Настройка через переменные окружения

Вместо файла конфигурации можно использовать переменные окружения:

```bash
export POSTMAN_API_KEY="your-api-key"
export POSTMAN_COLLECTION_ID="your-collection-id"

node scripts/sync-from-postman-api.js
node scripts/postman-to-openapi.js
```

## 📁 Структура файлов

```
.
├── scripts/
│   ├── sync-from-postman-api.js      # Загрузка из Postman API
│   ├── postman-to-openapi.js         # Конвертация в OpenAPI
│   └── sync-and-convert.js           # Универсальный скрипт
├── .github/
│   └── workflows/
│       └── sync-postman.yml          # GitHub Actions workflow
├── .postman-backups/                 # Резервные копии коллекций
├── postman-config.example.json        # Пример конфигурации
└── kinescope-api-openapi.yaml        # Результат конвертации
```

## 🔄 Процесс синхронизации

1. **Загрузка коллекции** из Postman API
2. **Сохранение резервной копии** в `.postman-backups/`
3. **Конвертация** в OpenAPI формат
4. **Сохранение** YAML и JSON файлов
5. **Автоматический коммит** (если есть изменения)

## 📊 Мониторинг

### Проверка последней синхронизации

```bash
# Посмотреть последнюю резервную копию
ls -lt .postman-backups/ | head -5

# Проверить дату последнего обновления OpenAPI
stat kinescope-api-openapi.yaml
```

### Логи GitHub Actions

Проверьте логи в разделе Actions вашего репозитория:
- ✅ Успешная синхронизация
- ⚠️ Предупреждения
- ❌ Ошибки

## 🛠️ Устранение неполадок

### Ошибка: "POSTMAN_API_KEY не установлен"

**Решение:**
1. Создайте `postman-config.json` с API ключом
2. Или установите переменную окружения: `export POSTMAN_API_KEY="your-key"`

### Ошибка: "Collection ID не найден"

**Решение:**
1. Проверьте правильность Collection ID в конфигурации
2. Убедитесь, что API ключ имеет доступ к коллекции

### Ошибка: "API ошибка 401"

**Решение:**
1. Проверьте правильность API ключа
2. Убедитесь, что ключ не истек
3. Проверьте права доступа к коллекции

### GitHub Actions не запускается

**Решение:**
1. Проверьте, что secrets добавлены в настройках репозитория
2. Убедитесь, что workflow файл находится в `.github/workflows/`
3. Проверьте синтаксис YAML файла

## 🔐 Безопасность

### ⚠️ Важные рекомендации:

1. **НЕ коммитьте** `postman-config.json` с реальными ключами
2. Добавьте в `.gitignore`:
   ```
   postman-config.json
   .postman-backups/
   .postman-collection-temp.json
   ```
3. Используйте **GitHub Secrets** для CI/CD
4. Регулярно **ротируйте API ключи**

## 📝 Примеры использования

### Локальная разработка

```bash
# 1. Обновите коллекцию в Postman
# 2. Экспортируйте коллекцию в JSON
# 3. Запустите конвертацию
node scripts/postman-to-openapi.js ~/Downloads/Kinescope\ API.postman_collection.json
```

### CI/CD пайплайн

```yaml
# В вашем CI/CD пайплайне
- name: Sync Postman
  env:
    POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}
    POSTMAN_COLLECTION_ID: ${{ secrets.POSTMAN_COLLECTION_ID }}
  run: |
    node scripts/sync-from-postman-api.js
    node scripts/postman-to-openapi.js
```

### Webhook от Postman (будущая функция)

Можно настроить webhook в Postman для автоматической синхронизации при изменении коллекции.

## 🎯 Best Practices

1. **Регулярная синхронизация** - настройте автоматический запуск
2. **Резервные копии** - система автоматически сохраняет копии
3. **Версионирование** - изменения коммитятся в git
4. **Мониторинг** - проверяйте логи синхронизации

## 📚 Дополнительные ресурсы

- [Postman API Documentation](https://www.postman.com/postman/workspace/postman-public-workspace/documentation/12959542-c8142d51-e97c-46b6-bd77-52c667e1becc)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [OpenAPI Specification](https://swagger.io/specification/)

---

**Готово! Теперь ваша документация будет автоматически синхронизироваться из Postman! 🎉**

