# Kinescope API - OpenAPI спецификация

Эта директория содержит OpenAPI 3.0 спецификацию для Kinescope API, автоматически сгенерированную из Postman коллекции.

## 📁 Файлы

- `kinescope-api-openapi.yaml` - OpenAPI спецификация в формате YAML (рекомендуется для чтения)
- `kinescope-api-openapi.json` - OpenAPI спецификация в формате JSON (для программного использования)
- `scripts/postman-to-openapi.js` - Скрипт для конвертации Postman коллекции в OpenAPI

## 🚀 Использование

### Для ИИ (ChatGPT, Claude, Cursor и др.)

OpenAPI спецификация идеально подходит для использования с ИИ-ассистентами:

1. **Загрузите файл в контекст**: Просто укажите путь к `kinescope-api-openapi.yaml` в вашем ИИ-ассистенте
2. **ИИ автоматически поймет структуру API**: Все endpoints, параметры, схемы запросов и ответов будут доступны
3. **Генерация кода**: ИИ сможет генерировать код для работы с API на любом языке

**Пример запроса к ИИ:**
```
Используя kinescope-api-openapi.yaml, создай функцию для получения списка видео 
с пагинацией и фильтрацией по статусу
```

### Для команды разработки

#### Просмотр документации

1. **Swagger UI** (веб-интерфейс):
   ```bash
   # Установите Swagger UI
   npm install -g swagger-ui-serve
   
   # Запустите сервер документации
   swagger-ui-serve kinescope-api-openapi.yaml
   ```
   Откройте http://localhost:3000 в браузере

2. **Redoc** (альтернативный интерфейс):
   ```bash
   npm install -g redoc-cli
   redoc-cli serve kinescope-api-openapi.yaml
   ```

3. **Онлайн просмотр**:
   - Загрузите файл на https://editor.swagger.io/
   - Или используйте https://redocly.github.io/redoc/

#### Генерация клиентов

OpenAPI спецификация позволяет автоматически генерировать клиенты для различных языков:

**JavaScript/TypeScript:**
```bash
npm install -g @openapitools/openapi-generator-cli
openapi-generator-cli generate -i kinescope-api-openapi.yaml -g typescript-axios -o ./kinescope-client-ts
```

**Python:**
```bash
openapi-generator-cli generate -i kinescope-api-openapi.yaml -g python -o ./kinescope-client-python
```

**Go:**
```bash
openapi-generator-cli generate -i kinescope-api-openapi.yaml -g go -o ./kinescope-client-go
```

**Другие языки:** См. полный список генераторов: https://openapi-generator.tech/docs/generators

#### Валидация запросов

Используйте OpenAPI спецификацию для валидации запросов в вашем коде:

```javascript
const Ajv = require('ajv');
const addFormats = require('ajv-formats');
const openApiSpec = require('./kinescope-api-openapi.json');

const ajv = new Ajv();
addFormats(ajv);

// Валидация request body
const validate = ajv.compile(openApiSpec.paths['/v1/videos'].post.requestBody.content['application/json'].schema);
const valid = validate(requestBody);
```

## 📊 Статистика

- **Endpoints**: 128
- **Paths**: 70 уникальных путей
- **Tags**: 33 тега (группы endpoints)
- **Версия OpenAPI**: 3.0.3

## 🔄 Обновление спецификации

Если Postman коллекция обновилась, запустите скрипт конвертации:

```bash
node scripts/postman-to-openapi.js
```

Скрипт автоматически:
- Парсит Postman коллекцию
- Извлекает все endpoints
- Конвертирует в OpenAPI формат
- Сохраняет в YAML и JSON

## 🔐 Аутентификация

Все endpoints требуют Bearer Token аутентификации:

```bash
curl -H "Authorization: Bearer YOUR_ACCESS_KEY" \
  https://api.kinescope.io/v1/videos
```

В OpenAPI спецификации это описано через `securitySchemes.bearerAuth`.

## 📝 Структура спецификации

### Основные разделы:

1. **info** - Информация об API (название, версия, описание)
2. **servers** - Базовые URL серверов (production и staging)
3. **tags** - Группировка endpoints по функциональности
4. **paths** - Все доступные endpoints с методами HTTP
5. **components** - Переиспользуемые компоненты:
   - `securitySchemes` - Схемы аутентификации
   - `schemas` - Модели данных (Error и др.)
   - `responses` - Стандартные ответы

### Пример endpoint:

```yaml
/v1/videos/{video_id}/posters:
  get:
    tags: [v1, videos, posters]
    summary: Posters list
    operationId: posters_list
    parameters:
      - name: video_id
        in: path
        required: true
        schema:
          type: string
    responses:
      '200':
        description: Успешный ответ
        content:
          application/json:
            schema:
              type: object
    security:
      - bearerAuth: []
```

## 🛠️ Интеграция с инструментами

### Postman

Импортируйте OpenAPI спецификацию в Postman:
1. Откройте Postman
2. Import → File → выберите `kinescope-api-openapi.yaml`
3. Все endpoints будут автоматически импортированы

### Insomnia

1. File → Import → From File
2. Выберите `kinescope-api-openapi.yaml`

### VS Code

Установите расширение "OpenAPI (Swagger) Editor" для подсветки синтаксиса и валидации.

## 📚 Дополнительные ресурсы

- [OpenAPI Specification](https://swagger.io/specification/)
- [Swagger Editor](https://editor.swagger.io/)
- [OpenAPI Generator](https://openapi-generator.tech/)
- [Kinescope Dashboard](https://kinescope.io)

## ⚠️ Примечания

- Спецификация автоматически генерируется из Postman коллекции
- Некоторые схемы ответов могут быть упрощены (используется inference из примеров)
- Для более точных схем рекомендуется дополнить спецификацию вручную
- Все примеры значений взяты из Postman коллекции

## 🤝 Вклад

Если вы нашли ошибки или хотите улучшить спецификацию:

1. Обновите Postman коллекцию
2. Запустите скрипт конвертации
3. Проверьте результат
4. При необходимости дополните спецификацию вручную

---

**Создано**: Автоматически из Postman коллекции Kinescope API  
**Последнее обновление**: См. дату изменения файлов  
**Версия API**: v1 и v2

