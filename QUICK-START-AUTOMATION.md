# ⚡ Быстрый старт автоматизации

## 🎯 За 5 минут настроить автоматическую синхронизацию

### Шаг 1: Получите Postman API Key (2 минуты)

1. Откройте https://go.postman.com/settings/me/api-keys
2. Нажмите "Generate API Key"
3. Скопируйте ключ

### Шаг 2: Найдите Collection ID (1 минута)

1. Откройте коллекцию в Postman
2. Нажмите на коллекцию → View → View Collection Info
3. Скопируйте **Collection ID**

### Шаг 3: Создайте конфигурацию (1 минута)

Создайте файл `postman-config.json`:

```json
{
  "postmanApiKey": "ваш-ключ-здесь",
  "collectionId": "ваш-id-здесь"
}
```

### Шаг 4: Запустите синхронизацию (1 минута)

```bash
npm run openapi:sync-api
```

Готово! 🎉

---

## 🔄 Автоматизация через GitHub Actions

### Настройка (один раз):

1. В GitHub репозитории: **Settings → Secrets → Actions**
2. Добавьте secrets:
   - `POSTMAN_API_KEY` = ваш Postman API ключ
   - `POSTMAN_COLLECTION_ID` = ваш Collection ID

3. Workflow автоматически запустится:
   - Ежедневно в 9:00 UTC
   - При изменении коллекции (если настроен webhook)
   - Вручную через Actions → Run workflow

---

## 📝 Ежедневное использование

### Команда обновляет методы в Postman → документация обновляется автоматически!

Ничего делать не нужно - GitHub Actions сделает всё сам.

### Ручной запуск (если нужно):

```bash
# Локально
npm run openapi:sync-api

# Или через GitHub Actions
# Actions → Sync Postman to OpenAPI → Run workflow
```

---

## ✅ Проверка работы

После первой синхронизации проверьте:

1. ✅ Файлы `kinescope-api-openapi.yaml` и `.json` обновлены
2. ✅ В папке `.postman-backups/` есть резервные копии
3. ✅ В GitHub Actions есть успешный запуск

---

**Всё готово! Документация теперь синхронизируется автоматически! 🚀**

