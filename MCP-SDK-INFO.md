# Информация о MCP TypeScript SDK

## Подтверждение использования официального SDK

Пакет `@mux/mcp` построен на основе **официального TypeScript SDK** от Model Context Protocol.

### Доказательства:

1. **Репозиторий SDK:**
   ```bash
   npm view @modelcontextprotocol/sdk repository
   # git+https://github.com/modelcontextprotocol/typescript-sdk.git
   ```

2. **Использование в коде @mux/mcp:**
   ```javascript
   // server.js
   const mcp_js_1 = require("@modelcontextprotocol/sdk/server/mcp.js");
   const types_js_1 = require("@modelcontextprotocol/sdk/types.js");
   
   // stdio.js
   const stdio_js_1 = require("@modelcontextprotocol/sdk/server/stdio.js");
   
   // http.js
   const streamableHttp_js_1 = require("@modelcontextprotocol/sdk/server/streamableHttp.js");
   ```

3. **Версии:**
   - В `@mux/mcp`: `@modelcontextprotocol/sdk@^1.11.5`
   - Последняя версия SDK: `1.24.3` (на момент проверки)

## Официальный репозиторий

**GitHub:** https://github.com/modelcontextprotocol/typescript-sdk

### Статистика:
- ⭐ **11k+ звезд**
- 🍴 **1.5k форков**
- 👥 **143 контрибьютора**
- 📦 **73 релиза**
- 📄 **MIT License**

### Что предоставляет SDK:

#### Для серверов:
- `McpServer` - базовый класс сервера
- `StdioServerTransport` - транспорт через stdin/stdout
- `StreamableHTTPServerTransport` - HTTP транспорт
- Обработка JSON-RPC протокола
- Поддержка Tools, Resources, Prompts
- OAuth авторизация
- Sampling и Elicitation

#### Для клиентов:
- Высокоуровневый клиент API
- Поддержка всех транспортов
- OAuth helpers
- Параллельные вызовы инструментов

### Документация SDK:

- **Локальная документация:**
  - `docs/server.md` - создание и запуск MCP серверов
  - `docs/client.md` - использование клиента
  - `docs/capabilities.md` - возможности (sampling, elicitation, tasks)
  - `docs/faq.md` - часто задаваемые вопросы

- **Внешние ссылки:**
  - [Model Context Protocol документация](https://modelcontextprotocol.io)
  - [MCP Specification](https://spec.modelcontextprotocol.io)
  - [Example Servers](https://github.com/modelcontextprotocol/servers)

### Примеры использования SDK:

SDK предоставляет множество примеров в `src/examples/`:

**Серверы:**
- Простой STDIO сервер
- HTTP сервер с Express
- OAuth защищенные серверы
- Серверы с sampling и tasks
- Elicitation примеры

**Клиенты:**
- Интерактивные клиенты
- Параллельные вызовы
- OAuth клиенты
- Обратная совместимость

## Архитектурная роль SDK в @mux/mcp

```
┌─────────────────────────────────────────┐
│         @mux/mcp (12.8.1)                │
│  (Mux API интеграция + инструменты)      │
└──────────────────┬──────────────────────┘
                   │ использует
                   ▼
┌─────────────────────────────────────────┐
│  @modelcontextprotocol/sdk (1.11.5)   │
│  (Официальный MCP SDK)                  │
│                                         │
│  - McpServer                            │
│  - Транспорты (stdio/http)             │
│  - JSON-RPC протокол                    │
│  - Базовые типы                         │
└─────────────────────────────────────────┘
```

### Что добавляет @mux/mcp поверх SDK:

1. **Интеграция с Mux API** через `@mux/mux-node`
2. **Автогенерация инструментов** из OpenAPI спецификации
3. **Адаптация под клиенты** (compat.js)
4. **Динамические инструменты** для экономии контекста
5. **Фильтрация инструментов** по ресурсам/операциям/тегам
6. **CLI интерфейс** для запуска сервера

## Вывод

`@mux/mcp` является **специализированной реализацией** MCP сервера для Mux API, построенной на официальном TypeScript SDK. Это правильный архитектурный подход:

✅ Использование проверенного официального SDK  
✅ Фокус на бизнес-логике (интеграция с Mux)  
✅ Переиспользование инфраструктуры  
✅ Соответствие стандартам MCP

